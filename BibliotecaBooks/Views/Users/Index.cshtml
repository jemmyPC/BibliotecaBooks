@model IEnumerable<Biblioteca.User>



@{
    ViewBag.Title = "Index";
}

<h2>List to Users Integon </h2>

<p>
    <button class="alert-info">  @Html.ActionLink("Create New", "Create")</button>
</p>
<br>
<br>
<div style="background-color:lightpink" >
    @using (Html.BeginForm())
    {
    <p>
         Find : @Html.TextBox("searchString")
        <input class="btn-warning col-lg-offset-8" type="submit" value="Search" b />
    </p>

    }
</div>
    <table class="table jumbotron form-group-lg table-hover" style="background-color:lightpink">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CURP)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CURP)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>


                <td>
                 <button class="alert-info"> @Html.ActionLink("Edit", "Edit", new { id = item.ID }) </button> |
                 <button class="alert-info"> @Html.ActionLink("Details", "Details", new { id = item.ID }) </button>  |

                </td>
            </tr>
        }

    </table>
